"""Narwhal SEO & GEO/LLMO scanner.

When installed as a package (via uvx/pip) these modules are importable as
``narwhal.*``; when run from a cloned repo or the Claude Code plugin, each script
also runs standalone (e.g. ``python scan.py <url>``).
"""

__version__ = "1.8.0"
